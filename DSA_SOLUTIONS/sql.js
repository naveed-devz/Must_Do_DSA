// select * from users;
// select name, email from userr;
// select * from users where email="snaveedn@gmail.com";
// select * from users limit(5)
// select * from users ORDER BY saalry desc


// select department count (*) as employee_count
// from employees
// group by department


// select department Avg (salary) as avg_salary    
// from employyes
// group by department


// select e.name d.department
// from employees e  
// JOIN department d on e.department_id = d.id

// select e.name d.department
// from employees
// LEFT JOIN d.department on e.department_id = d.id

// select e.name, d.department
// from employees e 
// RIGHT JOIN department d ON e.department_id = d.id